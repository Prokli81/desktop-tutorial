/**
 * Облако Sklad (опционально). По умолчанию выключено — данные только в браузере.
 * Для синхронизации телефон ↔ ПК скопируйте firebase-config.example.js и включите enabled.
 */
window.SKLAD_FIREBASE_CONFIG = {
  enabled: false,
};
