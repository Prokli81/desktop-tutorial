# AGENTS.md

## Cursor Cloud specific instructions

This repository is the GitHub **desktop-tutorial** starter: it contains only `README.md` and has no application source, package manifests, or service definitions.

- **Dependencies:** None to install. The VM update script is a no-op (`true`).
- **Lint / test / build:** Not applicable until application code is added.
- **Run:** Not applicable. The documented “first task” in `README.md` is to edit line 6 with your name and save via GitHub Desktop (or any Git workflow).
- **Git:** Standard `git` operations against `origin` are the only development workflow today.

When real code is added (e.g. `package.json`, `docker-compose.yml`, or a `Makefile`), extend this section with how to start required services and run lint/tests; keep the update script limited to dependency refresh only.
